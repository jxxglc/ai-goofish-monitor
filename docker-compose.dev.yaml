services:
  app:
    build: .
    container_name: ai-goofish-monitor-app
    init: true
    ports:
      - "9999:9999"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/dist
    restart: unless-stopped
